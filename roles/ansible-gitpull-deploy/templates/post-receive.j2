#!/bin/sh
git --work-tree="{{ git_checkout_location }}" --git-dir="{{ gitpush_repo_path }}" checkout -f
{% if gitpush_touch_path != '' %}
touch -r "{{ gitpush_repo_path }}" "{{ gitpush_touch_path }}"
{% endif %}
